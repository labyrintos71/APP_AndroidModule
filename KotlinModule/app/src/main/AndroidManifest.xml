<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="kotlins.module.labyrintos">
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.READ_CONTACTS"/>
    <uses-permission android:name="android.permission.WRITE_CONTACTS"/>
    <uses-permission android:name="android.permission.EXPAND_STATUS_BAR"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="kotlins.module.labyrintos.Network.RetrofitForCouroutine.ExamActivity"/>
        <activity android:name=".StatusBar.StatusBarSampleColorActivity"
            android:theme="@style/AppTheme.Color"/>
        <activity android:name=".StatusBar.StatusBarSampleTranslucentActivity"
            android:theme="@style/AppTheme.Translucent"/>
        <activity android:name=".BottomSheet.BottomSheetSampleActivity"/>
    </application>

</manifest>